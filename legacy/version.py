"""
version.py
캐시 무효화용 버전 상수 — 경량 모듈(표준 의존성 없음).

graph.py(=pyrosm/geopandas import)와 분리해 두어, 서비스가 캐시를 로드할 때
무거운 전처리 의존성 없이도 버전만 검증할 수 있게 한다.

가중치 공식이나 그래프 구성 로직이 바뀌면 이 값을 올려 캐시를 강제 무효화한다.
"""

CACHE_VERSION = 1
